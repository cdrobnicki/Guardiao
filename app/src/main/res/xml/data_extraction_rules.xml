<?xml version="1.0" encoding="utf-8"?>
<!--
  Nada do app é copiado para a nuvem nem transferido para outro aparelho.

  O banco da quarentena guarda os caminhos dos arquivos que o usuário considerou sensíveis — uma
  lista que diz onde estão os documentos pessoais dele. Essa lista não deve sair do aparelho, nem
  pelo backup do Google. Se o usuário trocar de celular, os arquivos continuam na pasta de
  quarentena que ele escolheu; só o histórico recomeça.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
